#include "main"
/**
 * print_alphabet - prints the alphabe, in lowercase, followed by a new line
 */
void print_alphabet(void)
{

	int ch = 'a';
		while (ch <= 'z')
		{
			_putchar(ch);
			ch++;
		}
	_putchar('\n');

return (0);
}
